<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>InfoHUB Home</title>
    <link rel="stylesheet" href="{{ asset('style.css') }}">
    <link rel="stylesheet" href="{{ asset('article.css') }}">
    <!-- Quill stylesheet -->
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <!-- Quill JavaScript -->
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
</head>

<body>
    <nav>
        <a href="/" class="InfoHUB">info<span class="hub">HUB</span></a>
        {# <a href="category.html">Learn</a>
        <a href="category.html">Music</a>
        <a href="category.html">Gaming</a>
        <a href="category.html">Blog</a> #}

  
        {% for category in categories %}
            <a href="{{ path('category', {'id': category.id}) }}">{{ category.name }}</a>
        {% endfor %}

            {% if user_data is defined and user_data is not null %}
        {{ user_data.username ?? '' }}

        {% if user_data.role is defined and user_data is not null %}
            {% if user_data.role == 'admin' %}
                Administrator Tito
            {% endif %}

            {% if user_data.role == 'editor' %}
                Editor Tito
            {% endif %}
        {% else %}
            User Tito
        {% endif %}

        <a href="/post/add">Add Post</a>
        <a href="/logout">Logout</a>
    {% else %}
            <a href="/register">Register</a>
            <a href="/login">Login</a>
    {% endif %}
    </nav>



<div id="wrapper">
